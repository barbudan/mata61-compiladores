nada W ( letra W [ ] , int W ) { real W [ ( A ( W , W ( ) ) / W / W % A ( ) * W * ( W - W ) - W ( ) + A ) ] ; int A [ W ( W - W * W * W ) ] ; se ( ( W ( ) / A ( ) ) + W ( ) % - A * W ( ) + A + A ( ) % A ( ) / W >= W ( ) ) { A ( A ( ) / A ( ) ) ; } senao { uai ( W <= A ou A ( ) <= A ( ) ) { le ( W ) ; A ( ) ; vaza ; } W = W ( ) % W ; } se ( ( ( W ( ) ) / A ( ) / A ( ) ) <= ( - A ) ) { } senao { escreve ( A ( ) / W ) ; tome A ; W ( ) ; } } int W ( int W , int A ) { letra W ; int W ; int W [ W ( ) * ( W ( ) ) / W * W ( ) / ( W / A ) ] ; letra A = A / A ( ) * A ( ) + ( A ( ) ) * A ( ) ; real A = A / A / W ; int A [ W * A ( ) ] ; int W = A ; letra W ; se ( W ( ) != ( W / A ( ) * A ( ) % ( A - A ( ) ) ) ) { } tome - W * W ( ) ; vaza ; escreve ( W ( ) % A ( ) - A ( ) * A ) ; escreve ( A ( ) * W ( ) ) ; } int mainha ( ) { real A = W [ ( A ( ) ) ] % - W * ( - A [ W ] ) ; real A [ - ( - A * - A ) ] ; letra A = - ( W - A ( ) ) ; A ( ) ; escreve ( - ( A ( ) * A ( ) ) ) ; }
