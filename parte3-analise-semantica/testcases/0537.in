nada H ( letra H , real H , letra H , real M , real H [ ] ) { letra M ; real H ; real M [ H [ M ( ) + H ( ) ] - - ( - M ) ] ; se ( M ( ) < ( ( M ( ) * H ) * H [ M ] ) ) { } senao { se ( H > H / H ) { le ( H ) ; H ( ) ; vaza ; } H [ H ( ) ] = H ; } M [ M * M + ( H ( ) ) * M % M ] = ( H ( ) * H * M ( ) ) - ( M ) ; } nada H ( letra M [ ] , int H ) { int M ; real M [ M ] ; escreve ( H ( H % M ( ) / H ( ) / M , H ( ) / M ( ) , M ( ) ) ) ; se ( H [ H * H ] >= H - M ( ) + H ( ) % H / M / H % H ( ) / M ( ) ) { } senao { } M = M ( ) ; H ( ) ; } int H ( real M , int M [ ] , real H [ ] , int M ) { letra H [ ( M ( ) * H / - M % ( ( M ( ) ) ) ) + H ( ) / H ( ) * M * - H ( ) % ( H ( ) ) % M ( ) + M ( ) * H * ( M ) ] ; letra H [ ( H ( ) ) ] ; int H [ - ( M ) + - H ( ) ] ; se ( M ( H ) <= - H ) { } senao { escreve ( ( M ) ) ; vaza ; vaza ; vaza ; vaza ; } escreve ( H ( ) / M / - H ( ) + M ) ; } real M ( int H , real M [ ] ) { int M = - ( - M ) + M % - H ( ) + ( H ) % ( M ) ; real H ; escreve ( H ( ) - ( ( M ) ) ) ; H ( M ( ) * H ) ; } int M ( letra H , letra M [ ] ) { letra M ; int M = M - H ( ) % H ( ) * H ( ) ; letra H ; real M ; vaza ; uai ( M ( ) == M ( ) ) { M ( ) ; vaza ; } } int mainha ( ) { real M ; int M [ - H ] ; int H ; int H ; M ( ) ; vaza ; H ( ) ; }
