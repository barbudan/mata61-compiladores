nada Q ( letra Q [ ] , int Q ) { real Q [ ( Q ( Q , Q ( ) ) / Q / Q % Q ( ) * Q * ( Q - Q ) - Q ( ) + Q ) ] ; int Q [ Q ( Q - Q * Q * Q ) ] ; se ( ( Q ( ) / Q ( ) ) + Q ( ) % - Q * Q ( ) + Q + Q ( ) % Q ( ) / Q >= Q ( ) ) { Q ( Q ( ) / Q ( ) ) ; } senao { uai ( Q <= Q ou Q ( ) <= Q ( ) ) { le ( Q ) ; Q ( ) ; vaza ; } Q = Q ( ) % Q ; } se ( ( ( Q ( ) ) / Q ( ) / Q ( ) ) <= ( - Q ) ) { } senao { escreve ( Q ( ) / Q ) ; tome Q ; Q ( ) ; } } int Q ( int Q , int Q ) { letra Q ; int Q ; int Q [ Q ( ) * ( Q ( ) ) / Q * Q ( ) / ( Q / Q ) ] ; letra Q = Q / Q ( ) * Q ( ) + ( Q ( ) ) * Q ( ) ; real Q = Q / Q / Q ; int Q [ Q * Q ( ) ] ; int Q = Q ; letra Q ; se ( Q ( ) != ( Q / Q ( ) * Q ( ) % ( Q - Q ( ) ) ) ) { } tome - Q * Q ( ) ; vaza ; escreve ( Q ( ) % Q ( ) - Q ( ) * Q ) ; escreve ( Q ( ) * Q ( ) ) ; } int mainha ( ) { real Q = Q [ ( Q ( ) ) ] % - Q * ( - Q [ Q ] ) ; real Q [ - ( - Q * - Q ) ] ; letra Q = - ( Q - Q ( ) ) ; Q ( ) ; escreve ( - ( Q ) ) ; }
