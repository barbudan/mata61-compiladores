int V ( int V [ ] , real V [ ] ) { letra V = V ; real V [ V ( ) * V ( ) % ( V ( ) ) / - V / V / V / V ( ) % V ( ) * V + - V ( ) * V * V / V ( ) + V ( ) - V ( ) / ( V ) - - V ( ) ] ; int V ; se ( V [ V ( ) ] + V [ V ] + - V ( ) <= V % V % - V ( ) - V ( ) % - ( V ) / ( V ) - V ( ) + V ( ) ) { le ( V [ V ( ) % V ] ) ; uai ( V ( ) > V ( ) ) { V ( ) ; } } senao { } escreve ( V [ V * V ( ) + - V ( ) ] ) ; } int V ( letra V [ ] , letra V , int V , letra V [ ] , letra V ) { real V ; letra V = V ( V ( ) - V ( ) % V * V , V * V , V ) ; letra V ; vaza ; uai ( ( V ( ) ) == - V ( ) e V % V ( ) >= - V ( ) e V % V ( ) % V <= V ( ) ) { } } nada V ( real V , real V [ ] ) { letra V = V ( ) ; letra V ; letra V ; V ( V ( ) ) ; le ( V [ ( V ( ) ) + V + V ] ) ; } letra V ( int V [ ] , real V [ ] , letra V [ ] , letra V ) { real V [ - V ( ) / V ( ) / V ( ) * - V ( ) * V ( ) ] ; letra V = V ( ) / V ( ) * V + V ; real V = ( V - V ) ; V = ( V ( ) + V ( ) ) * - ( V ( ) ) ; uai ( V - V == V ( ) * V ) { vaza ; } uai ( V == V ) { vaza ; } vaza ; vaza ; V ( ) ; vaza ; } int mainha ( ) { real V [ ( ( V % V ( ) ) ) ] ; int V [ ( V ( ) ) / ( V ) ] ; se ( V ( ) / V ( ) != V - V ) { le ( V ) ; V ( ) ; vaza ; } le ( V ) ; V = V ( ) ; le ( V ) ; V ( ) ; }
